RewriteEngine On
RewriteBase /api/

# Si no existe archivo o carpeta, redirige todo a index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
